name = "jess-oneill-website"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# KV bindings are configured in the Cloudflare Pages dashboard:
# Settings → Bindings → Add → KV namespace → OUTREACH_KV
